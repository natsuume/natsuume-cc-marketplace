{
  "description": "natsuume-writing の配送経路: SessionStart で文章作成一般と技術文書の執筆ルールの要点 (rules/core-summary.md) に rules ディレクトリの絶対パスを付けて additionalContext として常時注入する (inject-core.sh)。条件分岐なし。全文 (rules/general-writing.md / rules/writing-rules.md) は必要時に参照する。",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-core.sh",
            "args": []
          }
        ]
      }
    ]
  }
}
