{
  "description": "SessionStart で Bash コマンドを独立粒度で呼び出すよう Claude に指示する additionalContext を注入し、コマンド合成 (&&, ||, ;, $(...) 等) によって PreToolUse hook の検知を取りこぼすのを防ぐ",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-decompose-context.sh"
          }
        ]
      }
    ]
  }
}
