<!--
  ui-discipline: UI 実装規律 常時適用ルール (#197)
  各ルールは「意図 (なぜ) + 短い指示 + 境界 (いつ例外か)」で記述する
  (agent-discipline の always prompt と同じ記述形式)。

  ルール ID 一覧 (9 ルール、本文は Phase B で執筆する):
    rule:component-layers    — 層別の共通化基準
    rule:composition         — 共通 component の実装様式
    rule:component-search    — 実装前の既存探索
    rule:visibility-taxonomy — 表示/非表示・disabled の決定表
    rule:layout-stability    — レイアウト安定 (CLS 対策)
    rule:design-tokens       — token 経由のスタイル指定
    rule:a11y-basics         — アクセシビリティ基本則
    rule:async-states        — 非同期状態の網羅
    rule:robustness          — フォントサイズ・ビューポート頑健性
-->

# ui-discipline: UI 実装規律

以下のルールは UI (フロントエンド) の実装・変更時に常時適用される。

<!-- rule:component-layers -->
## 1. 層別の共通化基準

(Phase B で執筆)

<!-- rule:composition -->
## 2. 共通 component の実装様式

(Phase B で執筆)

<!-- rule:component-search -->
## 3. 実装前の既存探索

(Phase B で執筆)

<!-- rule:visibility-taxonomy -->
## 4. 表示/非表示・disabled の決定表

(Phase B で執筆)

<!-- rule:layout-stability -->
## 5. レイアウト安定 (CLS 対策)

(Phase B で執筆)

<!-- rule:design-tokens -->
## 6. token 経由のスタイル指定

(Phase B で執筆)

<!-- rule:a11y-basics -->
## 7. アクセシビリティ基本則

(Phase B で執筆)

<!-- rule:async-states -->
## 8. 非同期状態の網羅

(Phase B で執筆)

<!-- rule:robustness -->
## 9. フォントサイズ・ビューポート頑健性

(Phase B で執筆)
