{
  "description": "ui-discipline の配送経路: SessionStart で UI 実装規律 (hooks/prompts/ui-rules.md) を additionalContext として常時注入する (inject-ui-rules.sh)。モデル判定・permission_mode 判定等の条件分岐は持たず、常に同一内容を注入する (issue #197 の制約)。設計判断の詳細は plugin README を参照。",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-ui-rules.sh"
          }
        ]
      }
    ]
  }
}
