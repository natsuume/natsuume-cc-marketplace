{
  "description": "fable-discipline の 2 経路配送: SessionStart で Fable セッション向け分業規律 (誘導層) をハイブリッド方式で注入し session model を state file に記録 (inject-fable-role.sh)、PreToolUse (Agent|Task) でサブエージェントが Fable で実行される経路を deny (block-fable-subagent.sh、判定不能は fail-open)。判定順序・既知の制約の詳細は plugin README とスクリプトヘッダを参照。",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-fable-role.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent|Task",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/block-fable-subagent.sh"
          }
        ]
      }
    ]
  }
}
