{
  "description": "pre-merge-codex-review の配送経路: PreToolUse (Bash) で、codex review コメント (レビュー時 head SHA 記録付き) が PR に無い `gh pr merge` を deny (block-pre-merge.sh)、codex review wrapper の main session / background 起動を deny (block-bg-codex-wrapper.sh)。gate の判定内容・投稿 header 仕様は plugin README を参照。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/block-pre-merge.sh"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/block-bg-codex-wrapper.sh"
          }
        ]
      }
    ]
  }
}
