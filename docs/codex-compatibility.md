# Codex compatibility

<!-- Generated by scripts/sync_codex_marketplace.py. Do not edit directly. -->

Claude Code marketplace を version 以外の共有 metadata の正本とし、Skill・script・command hook は可能な限り同じ物理ファイルを共有します。Claude Code / Codex の plugin version は独立しており、Codex version、直接機構の差分、Codex 用の意図等価 adapter、残る保証差、検証面は `codex/marketplace-overrides.json` で個別管理されます。

| Plugin | Claude Code | Codex | Codex 配布 | 移植状態 | Skills | command hooks | 意図した差分 |
|---|---:|---:|---|---|---:|---:|---|
| `git-guardrails` | 0.5.3 | 0.5.3 | 対象 | 完全共有 | 1 | 3/3 | なし |
| `enforce-draft-pr` | 0.4.1 | 0.4.1 | 対象 | 完全共有 | 0 | 1/1 | なし |
| `auto-lint-check` | 0.5.1 | 0.5.1 | 対象 | 完全共有 | 0 | 4/4 | なし |
| `pre-push-review` | 4.2.1 | — | 対象外: 現行 Codex runtime の spawn_agent に agent_type selector が無く、agent_type=default の unauthenticated default agents を 3 reviewer として安全に識別できないため、fail-closed で marketplace 配布対象から除外します。 | メタデータのみ | 2 | 6/6 | Codex marketplace では pre-push-review の entry、manifest、Skill、hook を公開しないため、Codex から workflow を install・選択できません。<br>Codex 版 v3.1.4 以前の既存 install は marketplace からの除外だけでは local config/cache から削除されません。codex plugin remove pre-push-review@natsuume-plugins を実行し、新しい Codex thread を開始する必要があります。<br>Claude Code marketplace の plugin、command、agent、hook は引き続き利用できます。 |
| `update-default-branch` | 0.3.0 | 0.3.0 | 対象 | 完全共有 | 1 | 0/0 | なし |
| `natsuume-statusline` | 0.9.0 | 0.9.0 | 対象 | 一部変換 | 1 | 0/0 | Codex は任意 shell statusline、複数行表示、独自 gauge/color、statusline stdin callback を提供しないため、表示は組み込み項目で近似します。 |
| `agent-discipline` | 0.18.0 | 0.18.0 | 対象 | 一部変換 | 4 | 11/15 | semantic validator は既定で無効です。opt-in が無い状態では 4 つの literal gh issue/pr command を scoped deny し、nested Codex は起動しません。<br>opt-in 後は対象 gh issue/pr command ごとに別の codex exec を起動するため、ローカル Codex 認証・model availability・rate limit に依存し、同一 runtime handler より latency が増えます。hook payload と参照した --body-file 内容は親とは異なる provider へ送信される可能性があります。<br>Codex の PreToolUse は対応する Bash invocation の guardrail であり、env/cd prefix・editor・stdin 等の hook visibility 外経路を含む完全な shell security boundary ではありません。<br>Codex の常時 prompt は permission mode から Auto preset を推測せず、commit/push/issue/PR/merge をユーザー依頼 scope に含まれる場合だけ行います。auto-codex Skill は明示呼び出し時だけ follow-through の意図を追加します。<br>semantic validator は既定で gpt-5.6-sol を使います。AGENT_DISCIPLINE_CODEX_MODEL=gpt-5.6-luna で Luna に切り替えられますが、ローカル Codex の認証・model availability・rate limit に依存します。 |
| `ui-discipline` | 0.3.0 | 0.3.1 | 対象 | 一部変換 | 1 | 2/2 | Codex prompt は Claude prompt と別ファイルで保守するため、文言や質問 UI の逐語一致は保証せず、10 rule ID と受入結果の等価性を adapter test の範囲で保証します。 |
| `natsuume-writing` | 0.5.1 | 0.5.2 | 対象 | 一部変換 | 3 | 1/1 | Codex SessionStart prompt は共有 core-summary.md の逐語コピーではないため、常時注入文の完全一致は保証せず、詳細な媒体別規則は共有 Skills が読む writing-rules.md を正本とします。 |
| `codex-advisor` | 1.1.0 | — | 対象外: Claude Code から異種モデルの Codex へ相談するための plugin であり、Codex 自身から nested Codex を呼ぶ用途は marketplace 配布対象にしません。 | メタデータのみ | 1 | 8/8 | Codex marketplace では codex-advisor の entry、manifest、Skill、hook を公開しないため、Codex から $codex-advisor:consult を選択・install できません。<br>Claude Code marketplace では rescue / review / advisor を role 固有 runner に閉じ込め、main session の companion / wrapper 直接実行を許可しません。pre-push-review:codex-reviewer と一般 Codex review の合計が5回に達すると、根本方針 advisor checkpoint まで Stop と次の両 review も block します。 |
| `rate-limit` | 0.4.0 | 0.4.0 | 対象 | 一部変換 | 3 | 0/0 | Claude usage の status Skill は cross-provider 機能として利用できますが、Claude の認証情報または cache が必要です。<br>Codex は任意 statusline wrapper を持たないため、Claude/Codex cache の常時合成、独自 gauge、statusline 副作用は利用できません。 |
| `session-handoff` | 0.2.0 | 0.2.0 | 対象 | 一部変換 | 1 | 3/3 | Codex hook input は context 使用率を提供せず custom statusline callback も無いため、Claude 版と同じ 60% 閾値ではなく auto/manual compaction の直前に発火します。<br>transcript_path は安定 API として保証されず、summary 入力はサイズ上限内の末尾 excerpt に限定されます。<br>nested Codex は project config/MCP を隔離するため --ignore-user-config を使い、親 session の Azure/Bedrock/local/custom provider・account・data residency を継承または照合できません。repository 固有の inspect と明示承認 token による opt-in が無い限り producer は無音 no-op です。<br>nested Codex の認証・model/network・rate limit・timeout 失敗時は compaction を止めず fail-open となり、その回の handoff は保存されません。 |
| `fable-risk-labeler` | 0.1.0 | 0.1.0 | 対象 | 一部変換 | 1 | 0/0 | Claude marketplace でも共有 Skill は discovery されますが、判定主体を Codex に限定し、Claude / Fable session では write せず Codex での再実行を案内します。<br>semantic 判定は issue・repository・外部仕様の取得品質に依存し、将来の Fable 挙動や GitHub service の可用性を保証しません。 |
| `repo-analytics` | 0.1.0 | — | 対象外: レポート出力の中核である Artifact レポートと dataviz / artifact-design skill のロードが Claude Code 固有機能であり、Codex runtime では skill の必須出力を提供できないため、marketplace 配布対象から除外します。 | メタデータのみ | 1 | 0/0 | Codex marketplace に repo-analytics の entry と manifest を生成しないため、Codex から $repo-analytics:leadtime を選択・install できません。<br>Claude Code marketplace と Claude Code 版 skill は従来どおり利用できます。 |

## 保証差と検証テスト (Guarantee differences and verification tests)

保証差は、同じ利用目的を満たしていても Claude Code と同じ強制境界・発火時点・UI・モデル identity を保証できない点です。検証テストは adapter の入出力・lifecycle・install surface を検査しますが、LLM の意味判断品質そのものを証明するものではありません。

共通の前提:

- Codex の非 managed command hook は、利用前に `/hooks` で内容を確認して trust する必要があります。
- 現行 Codex の `PreToolUse` は Bash / `apply_patch` / MCP の対応経路に対する guardrail であり、同等操作を行うすべての shell 経路を捕捉する完全な security boundary ではありません。
- `SubagentStop` lifecycle は agent の終了と runtime identity を検証できますが、レビュー内容の意味的正しさを暗号学的に証明しません。
- Skill の自動選択意図は両 runtime が読む共通 `description` に集約し、runtime 固有 frontmatter は同期検証で拒否します。
- Claude marketplace 全体の `metadata.description` は Claude catalog 用です。Codex marketplace は `interface.displayName` と各 plugin description を生成しますが、marketplace 全体説明の同一 UI 表示は保証しません。

| Plugin | 残る保証差 | 検証テスト |
|---|---|---|
| `git-guardrails` | なし | `scripts/smoke_codex_marketplace.sh` — 生成 marketplace から plugin と共有 hook を Codex に install できること<br>`tests/test_git_guardrails_target_mismatch.py` — git global option と subcommand-local option を区別しつつ target-mismatch を fail-closed に deny すること<br>`tests/test_git_guardrails.py` — 3 hook が jq・library・必須関数の異常を非ゼロ終了と stderr 診断で可視化し、正常系では診断しないこと |
| `enforce-draft-pr` | なし | `tests/test_enforce_draft_pr.py` — draft 自動付与・明示的 non-draft deny、heredoc / opaque shell scope / redirection 境界、原文保持、性能契約<br>`scripts/smoke_codex_marketplace.sh` — 生成 marketplace から plugin と共有 hook を Codex に install できること |
| `auto-lint-check` | なし | `tests/test_auto_lint_codex_adapter.py` — apply_patch path 抽出、ignore 検知、Codex deny JSON の adapter 契約<br>`tests/test_auto_lint_commit_parser.py` — commit と cd の実行順に応じた repo override 判定契約 |
| `pre-push-review` | Codex 版の意図等価 adapter は提供しません。agent_type selector が実装され、runtime が named reviewer identity を lifecycle event で保証できるまで fail-closed の除外を維持します。 | `tests/test_pre_push_codex_adapter.py` — Codex marketplace からの除外、generic agent_type=default の marker 生成拒否、named agent type に限定した fail-closed 契約<br>`tests/test_pre_push_auto_mark.py` — Claude Code の SubagentStart launch attestation と SubagentStop での parent-safe report・hash 束縛検証、Codex pending attestation の final marker 昇格・失敗時破棄<br>`tests/test_pre_push_blocker.py` — push gate の fail-closed 入出力、worktree 診断、delete option 判定契約 |
| `update-default-branch` | なし | `scripts/smoke_codex_marketplace.sh` — 共有 Skill を含む plugin の Codex install surface |
| `natsuume-statusline` | stock Codex TUI では任意 shell callback・3 行 layout・独自 gauge/color を実行せず、固定 item ID の値と描画を Codex runtime が所有します。<br>strict config parse は TOML と既知 key を検査しますが status-line item ID の対応可否までは検査しないため、現在の /statusline picker を最終確認の正本とします。<br>setup-codex Skill は direct config 編集を検証済み backup 後の atomic replace、strict parse 失敗時の atomic restore、parse 成功時だけの完了報告に限定しますが、これは instruction contract であり専用 binary による hard security boundary ではありません。<br>context cache producer の副作用は statusline から再現せず、session-handoff plugin の PreCompact adapter に分離します。 | `tests/test_statusline_codex_adapter.py` — 組み込み status-line item、backup/atomic replace/strict parse failure/atomic restore の instruction contract、parse 検査境界と rendering 保証差<br>`scripts/smoke_codex_marketplace.sh` — pinned/latest Codex で strict config parser の unknown key 拒否と有効 config 受理 |
| `agent-discipline` | Codex manifest は codex/hooks.json を明示参照し、Claude のモデル分類・分割配送・temporary UI workaround・UserPromptSubmit prompt を runtime discovery から除外します。Codex prompt は 1 要素 8,000 文字以下を adapter test で固定します。<br>semantic validator は repository 外の一時 cwd で project document discovery、project root discovery、user/project config、hooks、rules、web search、shell/search tools を無効化し、対象 repository の AGENTS.md を validator policy から隔離します。Codex native policy は developer role、shell adapter が事前取得した inline/body file・branch・hook payload は user request の untrusted JSON として instruction hierarchy を分離します。<br>semantic validator は Codex 専用 prompt と全 property required の {ok, nullable reason} schema を使います。ok:true は stdout 無出力で通常の approval policy へ継続し、ok:false は PreToolUse deny に変換します。opt-in 後は認証・quota・timeout・strict config・schema/条件不適合を fail-closed で deny し、jq 不在または hook payload 自体を安全に解釈できない場合は blast radius を避けて no-op します。<br>owner-only regular marker と inspect→明示承認→action token の helper は当該 Git worktree で opt-in 操作が行われたことだけを示します。親/nested process の provider/model 同一性、Claude/Codex の verdict equality、外部 provider のデータ取扱いは保証しません。symlink・非 regular・owner/mode/content 不正 state は変更せず scoped deny します。<br>semantic interception は literal な gh issue/pr command head に限定され、同じ外部操作へ到達する全 shell 表現の遮断までは保証しません。<br>Codex の default・acceptEdits・dontAsk・bypassPermissions は Claude auto へ対応付けません。auto-codex Skill の follow-through 指示は Auto preset や追加権限を保証せず、外部 push/PR/merge はユーザー依頼 scope を拡張しません。 | `tests/test_agent_discipline_codex_adapter.py` — Claude/Codex hook 分離、Codex native semantic policy の developer role 分離、Sol/Luna model 制約、attached short flag・行継続を含む shell-pre-extracted body/branch、isolated cwd、hostile AGENTS/project config 非読込、web/shell/search tools 無効化、read-only/ephemeral validator、provider/privacy opt-in lifecycle、Structured Outputs subset、fail-closed deny、明示代替 Skills、Claude runtime 維持<br>`tests/test_codex_prompt_injection.py` — Sol/Luna 共通の短い Codex SessionStart/SubagentStart prompt、Claude 固有語の非配送、Goal/Context/Boundaries/Done when 契約、POSIX injector、Codex 専用 manifest |
| `ui-discipline` | Codex は codex/hooks.json から ${PLUGIN_ROOT} を使う command-only injector を読み、Claude 固有の AskUserQuestion・plugin root・SubagentStart 前置きを配送しません。<br>main agent では request_user_input が利用できる場合だけ上限内の 3 選択肢を使い、subagent は session prompt の後ろに置く override に従って未指定の open-ended visual direction を実装せず親 agent へ返します。 | `scripts/smoke_codex_marketplace.sh` — 共有 Skill と lifecycle hook を含む plugin の Codex install surface<br>`tests/test_codex_prompt_injection.py` — 10 rule ID、Sol/Luna prompt、3-option question、subagent override ordering、8K 上限、POSIX injector、Codex 専用 manifest |
| `natsuume-writing` | Codex は codex/hooks.json と ${PLUGIN_ROOT} を使う専用 injector から執筆 core を受け取り、Claude slash command 表記を配送しません。<br>詳細な outline/draft/review の規律は共有 Skill が rules/writing-rules.md を読むため、媒体別文体契約は両 runtime で同じです。 | `tests/test_sync_codex_marketplace.py` — 3 Skills が Claude Code と Codex の両 runtime surface を明示すること<br>`tests/test_codex_prompt_injection.py` — Sol/Luna prompt、Codex Skill 名、非執筆時の非適用、8K 上限、POSIX injector、Codex 専用 manifest |
| `codex-advisor` | Codex 版としての意図等価 adapter は提供せず、Claude Code が異種モデルの Codex を advisor にする本来の利用境界を維持します。 | `tests/test_codex_advisor_adapter.py` — Claude Code から利用する advisor wrapper の file-stdin companion、direct fallback、安全引数、deadline cleanup 契約<br>`tests/test_codex_advisor_subagent_runner.py` — Claude Code 固有の runner agents、direct gate、session lifecycle state、bounded retry、一般 / pre-push 共通の5 reviewごとの根本方針 advisor checkpoint、consult routing契約<br>`tests/test_sync_codex_marketplace.py` — codex-advisor が Codex marketplace と生成 manifest から除外され、Claude plugin 一覧には残ること |
| `rate-limit` | stock footer の limit 値・更新頻度・描画は Codex runtime が所有し、plugin script が値を差し替えたり Claude usage と合成したりできません。<br>strict config parse は TOML と既知 key を検査しますが limit item ID の対応可否までは検査しないため、現在の /statusline picker で最終確認します。<br>setup-codex Skill は direct config 編集を検証済み backup 後の atomic replace、strict parse 失敗時の atomic restore、parse 成功時だけの完了報告に限定しますが、これは instruction contract であり専用 binary による hard security boundary ではありません。<br>codex-status の詳細取得はローカル Codex 認証と app-server RPC の成功に依存し、取得失敗時は表示値を推測しません。 | `tests/test_statusline_codex_adapter.py` — 組み込み limit item、backup/atomic replace/strict parse failure/atomic restore の instruction contract、parse 検査境界、/usage、codex-status 導線<br>`scripts/smoke_codex_marketplace.sh` — codex-status/setup-codex の install surface と strict config parser の実行 |
| `session-handoff` | Claude 版の 60% early warning と user-driven handoff ではなく PreCompact 直前の自動生成なので、発火時点とユーザーが内容を確認する機会は同一ではありません。<br>Codex は親 session と同じ provider/account で要約する保証を構築できません。owner-only exact marker は越境リスクへの repository 単位の同意を表しますが、同一 provider や同一 data-residency の証明ではありません。<br>生成物は同一 git-dir 内で atomic 保存し compact 後に一度だけ注入しますが、LLM summary の内容品質と transcript 全履歴の包含は保証しません。<br>producer は privacy-safe default として無効です。enable 後も生成失敗は compaction 継続を優先する fail-open 契約であり、すべての compact で handoff が存在することは保証しません。 | `tests/test_session_handoff_codex_adapter.py` — 既定無効の no-read/no-call、read-only inspect、action-bound token enable/disable、exact owner-only marker と unsafe marker 拒否、単一 stdin prompt+transcript framing と codex exec '-'、tail 制限、nested Codex 隔離、atomic pending 保存、compact 後の once-only 注入、fail-open cleanup |
| `fable-risk-labeler` | Codex-only の判定主体制限、high-confidence rubric、write authorization、additive label API、再取得検証は Skill の instruction contract であり、専用 binary や GitHub policy による hard security boundary ではありません。<br>connected GitHub app が利用できない場合は認証済み gh CLI に fallback するため、tool 名・approval surface・認証経路は同一ではありません。 | `tests/test_fable_risk_labeler.py` — Codex-only instruction boundary、rubric の strong/non-signal、明示 write authorization、additive label 更新、置換・削除禁止、write 前後の evidence/verification contract |
| `repo-analytics` | Codex 版としての意図等価 adapter は提供せず、リードタイム分析は Claude Code セッションでのみ実行できます。 | `tests/test_sync_codex_marketplace.py` — repo-analytics が Codex marketplace と生成 manifest から除外され、Claude plugin 一覧には残ること<br>`tests/test_repo_analytics_leadtime.py` — leadtime 集計スクリプトの公開契約 (指標定義・境界挙動・fail-closed 終了コード) が runtime に依存せず検証されること |

## Component difference registry

この表にない Claude 固有の既定 component、plugin manifest / marketplace entry の component・enablement・依存宣言、非可搬 hook が追加されると CI は失敗します。未知の manifest / marketplace field と Codex 差分台帳の未知 field も、Codex での扱いを分類するまで fail-closed です。各行は Claude 正本 component（hook は matcher も含む）の SHA-256 fingerprint を固定します。さらに全 plugin は generator 所有の `.codex-plugin` と一時 cache を除く plugin tree 全体（README/docs を含む）と、宣言した verification test の内容を `sourceTreeDigest` に固定します。full 判定済み plugin も含め、既存 component・依存 script・保証説明・検証証跡の内容だけが変わった場合は adapter・保証差の再監査なしに CI を通しません。Disposition は直接ファイルの可搬性ではなく、意図の扱いまで含めて分類します。`adapted` は marketplace 内の代替実装、`surface-unavailable` は stock Codex surface では直接実行不能だが別の情報・workflow で近似可能、`host-required` は app-server 等の専用 host が必要、`not-applicable` は Claude 固有概念で Codex に適用対象がないことを示します。

| Plugin | Claude component | Source SHA-256 | Disposition | Codex replacement | Reason |
|---|---|---|---|---|---|
| `pre-push-review` | `commands/review.md` | `d934cb004193…` | `surface-unavailable` | — | 現行 Codex runtime は authenticated named reviewer を選択する agent_type selector を提供せず、plugin 自体を marketplace 配布対象外とするため、この command の Codex surface は公開しません。 |
| `pre-push-review` | `agents/code-reviewer.md` | `9420845734ab…` | `surface-unavailable` | — | 現行 Codex runtime では correctness reviewer の named identity を安全に選択・検証できず、Codex distribution から除外するため公開しません。 |
| `pre-push-review` | `agents/codex-reviewer.md` | `e72e6926bcea…` | `surface-unavailable` | — | 現行 Codex runtime では independent reviewer の named identity を安全に選択・検証できず、Codex distribution から除外するため公開しません。 |
| `pre-push-review` | `agents/security-reviewer.md` | `eab7f7185a5b…` | `surface-unavailable` | — | 現行 Codex runtime では security reviewer の named identity を安全に選択・検証できず、Codex distribution から除外するため公開しません。 |
| `pre-push-review` | `hooks/hooks.json#/hooks/PostToolUseFailure/0/hooks/0` | `be236d7412a9…` | `surface-unavailable` | — | Claude Code の Agent / Task failure payload に残る Codex pending attestation を破棄する lifecycle hook であり、plugin 自体を Codex distribution から除外しているため Codex surface は公開しません。 |
| `natsuume-statusline` | `commands/setup.md` | `fedf745cd1d0…` | `adapted` | `skills/setup-codex/SKILL.md` | Claude settings.json の statusLine.command 設定を Codex の /statusline と config.toml 手順に変換します。 |
| `natsuume-statusline` | `statusline/context-cache-dump.sh` | `0c584020d0d6…` | `surface-unavailable` | — | Codex に statusline stdin callback はありません。context handoff の意図は session-handoff の PreCompact adapter が担います。 |
| `natsuume-statusline` | `statusline/entrypoint.sh` | `e5ca162aa51a…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex に任意 shell statusline entrypoint がありません。 |
| `natsuume-statusline` | `statusline/gauges.sh` | `c82b07e30809…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex footer は独自 gauge renderer を受け付けません。 |
| `natsuume-statusline` | `statusline/lib.sh` | `d5b8ef0a4c8e…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | custom statusline runtime が無いため共有 helper の呼び出し先がありません。 |
| `natsuume-statusline` | `statusline/line1.sh` | `223204ab78ad…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex footer は任意の複数行 renderer を受け付けません。 |
| `natsuume-statusline` | `statusline/line2.sh` | `83bf3877e200…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex footer は任意の複数行 renderer を受け付けません。 |
| `natsuume-statusline` | `statusline/line3.sh` | `f1e0d4c06eae…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex footer は任意の複数行 renderer を受け付けません。 |
| `natsuume-statusline` | `statusline/main.sh` | `87c47294a210…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex に custom statusline process の実行面がありません。 |
| `natsuume-statusline` | `statusline/weekly-scoped-limits.sh` | `1529e071e2fc…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex footer の rate limit 表示は組み込み項目が所有します。 |
| `agent-discipline` | `hooks/hooks.json#/hooks/PreToolUse/0/hooks/0` | `14e28de43db3…` | `adapted` | `codex/prompts/semantic-validator.md` | repo/worktree scoped の明示 opt-in 後だけ、Codex 専用 semantic policy を developer role、shell-pre-extracted data を user request として階層分離した隔離 codex exec で評価し、ok:true を無出力継続、ok:false を PreToolUse deny に変換します。 |
| `agent-discipline` | `hooks/hooks.json#/hooks/PreToolUse/0/hooks/1` | `9956652d26bf…` | `adapted` | `codex/prompts/semantic-validator.md` | repo/worktree scoped の明示 opt-in 後だけ、Codex 専用 semantic policy を developer role、shell-pre-extracted data を user request として階層分離した隔離 codex exec で評価し、ok:true を無出力継続、ok:false を PreToolUse deny に変換します。 |
| `agent-discipline` | `hooks/hooks.json#/hooks/PreToolUse/0/hooks/2` | `d98f44419940…` | `adapted` | `codex/prompts/semantic-validator.md` | repo/worktree scoped の明示 opt-in 後だけ、Codex 専用 semantic policy を developer role、shell-pre-extracted data を user request として階層分離した隔離 codex exec で評価し、ok:true を無出力継続、ok:false を PreToolUse deny に変換します。 |
| `agent-discipline` | `hooks/hooks.json#/hooks/PreToolUse/0/hooks/3` | `7dc1aa84a3f6…` | `adapted` | `codex/prompts/semantic-validator.md` | repo/worktree scoped の明示 opt-in 後だけ、Codex 専用 semantic policy を developer role、shell-pre-extracted data を user request として階層分離した隔離 codex exec で評価し、ok:true を無出力継続、ok:false を PreToolUse deny に変換します。 |
| `agent-discipline` | `hooks/hooks.json#/hooks/PreToolUse/1/hooks/0` | `a23b1862a792…` | `not-applicable` | — | Agent\|Task は Claude 固有 tool matcher で、Fable subagent block は Codex model 系に適用されません。 |
| `codex-advisor` | `agents/advisor-runner.md` | `cd7fd21b6568…` | `surface-unavailable` | — | Claude Code の Agent tool と companion job を接続し、5 review ごとの根本方針 checkpoint attestation も返す role 固有 runner であり、plugin 自体を Codex distribution から除外しているため公開しません。 |
| `codex-advisor` | `agents/rescue-runner.md` | `684a9495ec11…` | `surface-unavailable` | — | Claude Code の legacy rescue 経路を named Agent へ reroute する runner であり、plugin 自体を Codex distribution から除外しているため公開しません。 |
| `codex-advisor` | `agents/review-runner.md` | `c69fca238b9a…` | `surface-unavailable` | — | Claude Code の Bash / TaskOutput tracking 喪失を companion job 集合差分から復旧し、成功 review を pre-push Codex review と共有する cadence counter へ接続する runner であり、plugin 自体を Codex distribution から除外しているため公開しません。 |
| `codex-advisor` | `hooks/hooks.json#/hooks/SessionEnd/0/hooks/0` | `f5e61eb80fba…` | `surface-unavailable` | — | Claude Code session の runner state を終了時に掃除する hook であり、plugin 自体を Codex distribution から除外しているため公開しません。 |
| `rate-limit` | `commands/setup.md` | `dc92c470b8a6…` | `adapted` | `skills/setup-codex/SKILL.md` | Claude statusLine wrapper setup を Codex の組み込み limit footer と /usage の setup に意訳します。 |
| `rate-limit` | `statusline/cache-write-wrapper.sh` | `017064e4f602…` | `surface-unavailable` | `skills/setup-codex/SKILL.md` | Codex は任意 statusline wrapper command を実行しません。 |

## Synchronization contract

- Shared metadata and Claude Code versions: `.claude-plugin/marketplace.json` and `plugins/*/.claude-plugin/plugin.json`
- Codex distribution, versions, and Codex-specific registry: `codex/marketplace-overrides.json`
- Runtime version policy: paths declared in `versioning.claudeOnlyPaths` / `codexOnlyPaths` bump only that runtime; unclassified plugin paths are shared and require both versions to advance
- Generated artifacts: `.agents/plugins/marketplace.json`, `plugins/*/.codex-plugin/plugin.json`, `AGENTS.md`, this document
- Runtime validation: pinned Claude Code strict-validates the canonical marketplace; pinned Codex installs every generated plugin on each PR/push, and scheduled latest-version jobs act as compatibility canaries
- Every plugin entry declares `guaranteeDifferences` and at least one `verificationTests[].path`; each path must be executed by unittest discovery or the marketplace smoke job, so CI rejects missing or unexecuted evidence declarations
- Every non-portable Claude component pins `sourceDigest`; every plugin pins `sourceTreeDigest` for its behavior tree and declared test evidence. After reviewing every stale plugin printed together by `--check`, run `python3 scripts/sync_codex_marketplace.py --refresh-source-digests --plugin <name> [--plugin <name> ...]` with the exact complete set for a no-write preview. Apply only by repeating it with the state-bound `--approve <action-token>` printed by that preview; source, test, registry, or marketplace byte changes (including formatting-only registry edits) invalidate the token.
- Regenerate: `python3 scripts/sync_codex_marketplace.py --write`
- Verify without writes: `python3 scripts/sync_codex_marketplace.py --check`
